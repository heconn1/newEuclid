# C build artifacts
src/*.o
/euclid
result

# Chapel build artifacts
/euclid_chpl
/euclid_chpl_debug
*_real
.dSYM/
/tests/build/

# Generated/scratch field data (intentional saved fixtures live in
# tests/fixtures/ and are tracked)
/field_data.txt
/field_data.txt.old

# sqlite database (only used if SQLITE=yes in Makefile)
db/*.db
